'use client';

/**
 * Simplified Analytics Provider
 * 
 * This is a placeholder component that will be replaced with proper analytics
 * functionality when the site is ready for production.
 */
export default function AnalyticsProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
