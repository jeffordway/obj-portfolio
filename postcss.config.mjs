/**
 * PostCSS Configuration
 * 
 * This file configures PostCSS plugins for the project,
 * primarily for processing Tailwind CSS.
 */

const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
